Write a C program that takes exactly three system commands as arguments and executes them 
such that the standard output of the first command is piped into the standard input of the 
second, and the standard output of the second is piped into the standard input of the third.